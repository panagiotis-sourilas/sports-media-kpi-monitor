name: 'sports_media_kpi_monitor'
version: '1.0.0'
config-version: 2

profile: 'sports_media_kpi_monitor'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  sports_media_kpi_monitor:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts

seeds:
  sports_media_kpi_monitor:
    +schema: seeds
